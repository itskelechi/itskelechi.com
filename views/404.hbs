<div  id="err404">
    
    <div class="err-msg">
        <h1>Error: </h1>
        <h2>lol, there's nothing here</h1>
    </div>

    <div class="return-home">
        <a href="/home" class="go-home">
            <div class="e-container" style="width: 100%; height: 100%;">
                <img src="./SiteImages/icons/return-home-icon.png" class="go-home"/>
                {{!-- <span class="tooltiptext">Go Home</span> --}}

                {{!-- curved text --}}
                <svg width="200" height="120" viewBox="0 0 200 120">
                    <path id="curve" d="M40,80 A85,75 0 0,1 160,80" fill="transparent"/>
                        <text class="text" fill="white">
                            <textPath href="#curve" startOffset="50%" text-anchor="middle" style="text-shadow: 2px 2px 0px var(--hunter-green); letter-spacing: 1px;">RETURN HOME
                            </textPath>
                        </text>
                </svg>
            </div>
        </a>
    </div>

    <div class="stop">
        <h3>Really Stop!</h3>
        <h3>What are you? The US Government?</h3>
    </div>

    <div class="tooltip">
        <p class="hm-redirect tooltip">
            <a href="/home">(Follow the stones to the transporter)</a>
            <span class="tooltiptext">or click here - if lazy</span>
        </p>
    </div>

    <p id="last">ladidi ladida</p>

</div>

<style>
    #err404{
        text-align: end;
        align-items: end;
        min-width: 40vw;

    }
    .e-container{
        position: relative;
        width: 100%;
        height: 100%;
    }
    div{
        display: flex;
        flex-direction: column;
    }
    h1, h2 {
        top: 0;
        margin-bottom: 0;
        padding-bottom: 0;
    }
    h1{
        font-size: clamp(24px, 6vw, 32px);
        color: var(--white);
        text-shadow: 3px 3px 0px var(--hunter-green);
    }
    h2{
        font-size: clamp(18px, 6vw, 24px);
        margin-left: 50px;
        width: 350px;
        color: var(--white);
        text-shadow: 3px 3px 0px var(--hunter-green);
    }
    h3,h4,p {
        color: white;
        font-size: clamp(16px, 6vw, 20px);
    }
    /* Base Layout */
#err404 {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    padding: 2rem;
    z-index: 0;
    min-width: 490px;
}

/* Responsive Font Scaling */

.text{
    font-family: "Silkscreen",sans-serif;
    font-size: clamp(12px, 1.5vw, 16px);
}

    .err-msg{
        position: fixed;
        top: 2%;
        right: 5%;
    }
    .stop{
        color: white;
        position: fixed;
        bottom: 18%;
        right: 5%;
        min-width: 250px;
    }
    img.go-home{
        width: clamp(75px, 8vw, 195px);
        position: absolute;
        top: 50%;
        left: 50%;
        z-index: 5;
        transform: translate(-50%, -50%);
    }
    a.go-home{
        display: inline-flex;
        align-items: center;
        position: absolute;
        filter: drop-shadow(0px 0px 8px var(--light-mauve))
                drop-shadow(0px 0px 16px var(--light-mauve))
                drop-shadow(0px 0px 24px var(--light-mauve))
                drop-shadow(0px 0px 32px var(--light-mauve));

    }
    .tooltip{
        bottom: 10%;
        right: 5%;
        font-size: 14px;
    }
    .return-home{
        position: fixed;
        top: 50%; 
        right: max(52%, min(50px, calc(90% - (100vw - 705px))));        
        transform: translateY(-50%);
        text-align: center;
        width: min(200px, 30vw);
        height: 100px;
        transition: all 0.3s ease;
    }
    svg{
        position: absolute;
        top: calc(50% - 75px); /* Adjust this value as needed */
        left: 50%;
        transform: translate(-50%, -75%);
        width: 200px;
        height: 120px;
        z-index: 4
    }


</style>